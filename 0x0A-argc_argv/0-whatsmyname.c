#include "main.h"
#include <stdio.h>
/**
 * main - print the name of the program
 * @argc: number of comaand line arguments
 * @argv: array name
 * Return: 0
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[argc * 0]);
return (0);
}
